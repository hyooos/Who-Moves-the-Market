# 연속 게시물 사건 clustering 검증

## 문제

시간대 수정 결과에는 시장 관련 게시물 3,015건이 모두 개별 이벤트로 들어갔습니다. 그러나 같은 화자와 ticker의 게시물이 24시간 안에 겹쳐 `multi_post_flag=True`가 된 행이 2,608건(86.5%)이었습니다. 같은 가격 반응을 연속 게시물 수만큼 반복 관측하거나, 반대로 대부분을 오염 표본으로 버리는 문제가 동시에 있었습니다.

## 선택한 기준

- 그룹: `person + ticker + topic + event_date`
- 시간: 각 cluster의 첫 게시물 기준 6시간 고정 창
- 대표 시각: cluster의 첫 게시물
- 가격 반응: 해당 ticker·event_date의 한 번의 반응
- 보존: 모든 원문 ID·URL·UTC/ET 시각·본문을 `member_*_json` 컬럼에 저장

직전 게시물과의 간격을 기준으로 하지 않은 이유는 5시간마다 글이 이어질 경우 하나의 cluster가 며칠까지 늘어나는 chaining 문제를 막기 위해서입니다.

## 기준 비교

| 고정 창 | 사건 수 | 게시물 대비 감소 | 여러 글을 포함한 사건 | CLEAN 예상 |
| ---: | ---: | ---: | ---: | ---: |
| 3시간 | 2,297 | 23.8% | 446 | 408 |
| 6시간 | 2,173 | 27.9% | 511 | 429 |
| 12시간 | 2,023 | 32.9% | 563 | 474 |

3시간은 같은 캠페인을 지나치게 나눌 가능성이 있고, 12시간은 장 전·장중·장 후의 별도 발언을 과하게 합칠 위험이 커 기본값은 6시간으로 정했습니다. 최종 연구에서는 3시간·12시간 결과를 민감도 분석으로 함께 제시하는 것이 안전합니다.

## 6시간 기준 전체 검증

| 항목 | 결과 |
| --- | ---: |
| 원본 게시물 | 3,015 |
| cluster 사건 | 2,173 |
| singleton 사건 | 1,662 |
| 여러 게시물을 포함한 사건 | 511 |
| 최대 cluster 크기 | 33개 게시물 |
| CLEAN | 429 |
| MINOR | 1,502 |
| MAJOR | 242 |

모든 cluster의 `cluster_size` 합은 3,015이며, 원본 source ID 3,015개와 URL 3,015개가 손실 없이 보존되는 것을 확인했습니다. clustering 후 가설검정 수치는 Colab 전체 파이프라인을 다시 실행해 확정해야 합니다.

Track2의 `00:00:00` 자리표시 시각은 실제 장전 게시로 단정하지 않고 `DATE_ONLY_MANUAL_REVIEW`로 표시하도록 함께 수정했습니다. 정확한 시각이 확인되면 `timestamp_precision=exact`와 `posted_at_timezone`을 수동 CSV에 명시해야 합니다.

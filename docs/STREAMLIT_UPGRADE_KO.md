# Streamlit 서비스 화면 안내

## 바로 실행

- Windows: `run_streamlit.bat`를 더블클릭합니다. 검은 창은 서버이므로 서비스를 사용하는 동안 닫지 않습니다.
- macOS/Linux: 터미널에서 `bash run_streamlit.sh`를 실행합니다.
- 브라우저가 자동으로 열리지 않으면 `http://localhost:8501`을 직접 엽니다.

최초 실행에는 가상환경과 패키지 설치 시간이 필요합니다. 데이터가 없다면 먼저 `run_daily_pipeline.py`를 실행해야 합니다. 게시물 감성을 화면에 표시하려면 `--add-sentiment` 옵션을 포함합니다.

화면 상단의 큰 요약 카드에는 `현재 분석 기간`을 표시합니다. 전체 데이터 범위는 바로 아래 `분석 기간 설정` 영역에 따로 안내하며, 그 안에서 시작일과 종료일을 바꿀 수 있습니다. 선택 범위는 시장 반응·사건 찾기·데이터 질문에 함께 적용되며, 평소 대비 반응은 비교 자료가 지나치게 줄지 않도록 전체 과거 SNS 사건을 기준으로 유지합니다.

## 화면 구조

### 1. 시장 반응

SNS 원문 사건 또는 뉴스로 확인한 추가 사례를 선택합니다. 먼저 시장별 최신 사건 계기판을 확인한 뒤 `주가와 사건 타임라인`에서 시장과 인물을 고를 수 있습니다. 종가 흐름 위의 점을 클릭하면 선택 날짜·해당일 종가·사건 목록이 그래프 바로 아래에 넓게 표시되고, 선택한 사건의 상세 카드로 이어집니다. 한 날짜에 사건이 여러 건이면 목록에서 하나를 고를 수 있습니다.

머스크·트럼프 캐릭터가 표시된 계기판에서는 테슬라, 나스닥 100, S&P 500의 가장 최근 사건 반응이 같은 시장의 과거 SNS 사건 중 어느 수준인지 확인할 수 있습니다. 계기판에는 핵심 수치와 짧은 미리보기만 표시하며, 미리보기는 문장 중간이 잘려 보이지 않도록 말줄임표로 마무리합니다. 전체 내용은 타임라인 아래 사건 상세에서 확인합니다. 뉴스 사례에 `비교 자료 부족`이 표시되던 문제는 뉴스끼리만 비교하던 기준선 오류였으며, 뉴스 크롤링 여부와는 관련이 없습니다.

사건을 선택하면 다음 순서로 보여줍니다.

1. 실제 등락률
2. 같은 날 시장 전체 움직임을 제외한 등락률
3. 같은 시장의 과거 사건 대비 반응 수준
4. 원문의 감성(긍정·중립·부정)과 신뢰도
5. 원문·한국어 번역·사건 요약·전후 주가

뉴스 사례는 `뉴스로 확인한 추가 사례`로 표시해 SNS 원문 사건과 섞이지 않게 했습니다. 뉴스·기자회견·수동 등록 사례는 SNS 문장 감성분석 대상이 아니므로 `분석 전`이 아니라 `감성 미적용`으로 표시합니다.

### 트럼프 최신 Truth Social 게시물

`시장 반응` 화면의 `최신 게시물 조회`를 열고 `트럼프 최신 게시물 확인` 버튼을 누르면 비공식 Truth Social 미러 RSS에서 목록을 가져온 뒤 Jina Reader로 각 게시물 본문을 읽습니다.

- API 키나 Bearer Token이 필요하지 않습니다.
- 최근 24시간에 게시물이 없으면 3일·5일·7일 순으로 조회 범위를 넓힙니다.
- 버튼을 누를 때만 조회하며 같은 결과는 5분 동안 캐시합니다.
- 새 게시물은 가격 반응이 아직 확정되지 않았으므로 조회용으로만 표시하고 기존 계기판 분석에는 자동으로 넣지 않습니다.
- 비공식 미러 RSS와 Jina Reader의 형식·가용성에 따라 일시적으로 조회가 실패할 수 있습니다.
- Musk는 무료로 안정적인 실시간 X 소스가 없어 화면에서 지원하지 않습니다. 과거 게시물 대량 수집은 별도 `twscrape` 백필 스크립트의 역할입니다.

### 2. 사건 찾기

인물, 시장, 자료 형태, 게시물 분위기, 키워드로 사건을 좁힙니다. 표에서 사건을 고르면 첫 화면과 같은 상세 카드와 전후 주가를 확인할 수 있습니다.

### 3. 데이터 질문

질문에서 인물·시장·주제·자료 형태를 찾아 실제 사건 데이터를 먼저 계산합니다. 다음 질문은 AI 연결 없이도 서로 다른 계산 결과로 답합니다.

- 반응이 가장 컸던 사건
- 일론 머스크와 도널드 트럼프 비교
- 테슬라·나스닥 100·S&P 500 비교
- 긍정·중립·부정 게시물의 시장 반응 비교
- 사건 수와 데이터 기간

AI를 연결하면 계산 결과를 자연어로 더 유연하게 설명하고, 연결에 실패하면 자동으로 기본 데이터 답변을 사용합니다.

## AI 요약과 한국어 번역

- `사건 요약 생성` 버튼은 항상 사용할 수 있습니다. AI가 없으면 원문과 계산값으로 기본 요약을 만듭니다.
- 영어 원문일 때만 `원문 한국어로 번역` 버튼이 표시됩니다. 이미 한국어인 뉴스 설명에는 별도 번역 버튼을 두지 않습니다.
- 한국어 번역은 상단 `AI 설정`에서 Gemini·Groq·Ollama 중 하나를 연결하면 사용할 수 있습니다.
- AI가 없을 때는 `기본 온라인 번역 허용`을 직접 선택한 경우에만 원문을 온라인 번역 서비스로 보냅니다.
- 같은 입력의 결과는 캐시해 불필요한 반복 호출을 줄입니다.

AI가 주가 수치를 새로 만들지는 않습니다. 등락률·반응 수준·감성 값은 파이프라인 결과에서 가져오고, AI는 번역과 설명만 담당합니다.

## 감성분석 표시

감성분석은 Track1 SNS 사건으로 묶인 게시물 전체 문장을 기준으로 실행됩니다. 결과 파일에 감성 값이 있으면 첫 화면의 `게시물 분위기별 빠른 보기`와 사건 상세 카드에 긍정·중립·부정 및 신뢰도가 표시됩니다. Track2 뉴스·수동 사례는 문장 출처와 성격이 달라 감성 비교 대상에서 제외합니다.

```powershell
.venv\Scripts\python.exe run_daily_pipeline.py --add-sentiment --add-novelty --run-placebo --run-rivn-sensitivity
```

감성 모델 설치가 필요하면 다음을 먼저 실행합니다.

```powershell
.venv\Scripts\python.exe -m pip install -r requirements-optional.txt
```

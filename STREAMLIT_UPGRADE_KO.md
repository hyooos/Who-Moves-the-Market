# 대시보드 화면 안내

## 바로 실행

- Windows: `run_streamlit.bat` 더블클릭. 검은 창은 서버이므로 대시보드를 사용하는 동안 닫지 않습니다.
- macOS/Linux: 터미널에서 `bash run_streamlit.sh`

최초 실행만 가상환경과 패키지 설치 시간이 필요합니다. 두 번째 실행부터는 필요한 패키지가 이미 있는지 빠르게 확인한 뒤 같은 환경을 재사용합니다. 브라우저가 자동으로 열리지 않으면 `http://localhost:8501`을 직접 열면 됩니다.

## 화면 구조

- `실시간 메인`: 기존 계기판과 일론 머스크·도널드 트럼프 캐릭터, 주가 차트, 오른쪽 미니 챗봇을 함께 확인
- `개요`: 전체 사건 범위, 반응 분포, 반응 강도가 컸던 사건 확인
- `이벤트 탐색기`: 인물·주제·관련 시장·원인 구분 가능성으로 사건을 찾고 상세 내용 확인
- `가설 검증`: 연구 질문별 결론과 보정된 검정 수치 확인
- `방법론 점검`: 다른 게시물·경제 일정·시장 충격이 겹쳤는지 확인
- `케이스 스터디`: 2025년 4월 이후 뉴스 사건과 장중 분석 자료 확인
- `Ask the Data`: 자연어 질문을 실제 사건 조건으로 바꿔 계산한 뒤 결과를 설명

상단 제목과 탭 메뉴는 원본 `Who Moves the Market?` 구성을 유지하고, 카드·간격·색상·표현만 정돈했습니다. 전체 챗봇 메뉴는 그대로 두고 실시간 메인 오른쪽에도 같은 계산 로직을 사용하는 작은 챗봇을 추가했습니다.

## 사건 클릭 후 작동 원리

1. 차트에서 선택한 점과 연결된 사건을 내부 식별자로 찾습니다. 내부 식별자는 화면에 노출하지 않고 `사건 0001` 형태로 표시합니다.
2. 원문·인물·주제·관련 시장·미국 동부시간 기준 게시 시각을 표시합니다.
3. 실제 등락률, 시장 대비 등락률, 반응 강도, 원인 구분 가능성을 원본 분석 결과에서 계산해 표시합니다.
4. 전후 15일 가격을 읽어 캔들 차트와 5일 이동평균을 그립니다.
5. 번역과 AI 요약은 버튼을 눌렀을 때만 선택한 AI 서비스를 호출합니다.

따라서 AI가 수치를 새로 만들지 않습니다. 숫자는 분석 결과 파일에서 가져오고 AI는 원문 번역과 계산 결과 설명만 담당합니다.

## 속도 개선

- 게이지 원문 자동 번역 제거
- 사건 클릭 시 기사 자동 다운로드 제거
- 사건 클릭 시 AI 분석 자동 호출 제거
- 기사·번역·분석 결과를 기사 주소·본문·AI 서비스·사용 모델 기준으로 임시 저장
- 데이터 파일은 `st.cache_data`로 한 번만 로드
- AI 인증 키가 없어도 계산·차트·필터는 즉시 사용 가능

## 데이터에 질문하기

질문에 포함된 다음 조건을 먼저 탐지합니다.

- 인물: 일론 머스크, 도널드 트럼프
- 관련 시장: 테슬라, 나스닥 100, S&P 500
- 주제: 인공지능, 관세, 중국 관련, 테슬라 사업 등
- 원인 구분: 원인 비교적 명확

그다음 조건에 맞는 사건 수, 원인을 비교적 구분할 수 있는 사건 수, 인물·관련 시장·주제별 평균과 중앙값, 반응 강도 상위 사건을 계산합니다. AI에는 이 계산 결과와 화면용 사건 번호만 전달합니다. 답변 아래 `답변에 사용한 계산 근거`에서 실제 상위 사건을 확인할 수 있습니다.
